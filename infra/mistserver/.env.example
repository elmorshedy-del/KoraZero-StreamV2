# No provider credentials belong in this file.
# Production secrets will be injected by the deployment platform when
# the V2 source-control service is implemented.
V2_PUBLIC_HLS_BASE=http://localhost:8080/hls
